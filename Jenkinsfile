pipeline {
    agent any
    stages {
        stage('Build and List') {
            steps {
                echo 'Running from the Jenkinsfile  on main branch in Git!'
                sh 'ls -R'
            }
        }
    }
}

pipeline {
    agent any
    stages {
        stage('Build and List') {
            steps {
                echo 'Running from the Jenkinsfile  on dev branch in Git!'
                sh 'ls '
            }
        }
    }
}
